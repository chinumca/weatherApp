import { ThumbHoverDirective } from './thumb-hover.directive';

describe('ThumbHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbHoverDirective();
    expect(directive).toBeTruthy();
  });
});
