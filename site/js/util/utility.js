/**
 * This javascript file is used as utility library.
 * You can write you custom logic in functions and make code re-usable.
 * This file is linked in index.html
 * functions/methods written here are global, so you can access directly.
 * @author Sachin Patil (sachinpatil_mca@yahoo.in)
 */
 
 /* below code helps javascript's sort method to sort array of objects wrt objects property.*/
	function sortObjectsArray(a,b) {
		if (a < b)
		return 1;
		if (a > b)
		return -1;
		return 0;
	};
					
					
					